import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.nurani.maktab',
  appName: 'নূরানী মক্তব',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
